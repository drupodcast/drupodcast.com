version: 1

notification:
  provider: slack

steps:
  develop:
    prepare:
      - composer validate
      - composer install --no-progress --prefer-dist --no-dev --optimize-autoloader
    process:
      - vendor/bin/drupal build:stage
    finalize:
      - vendor/bin/drupal cr all
